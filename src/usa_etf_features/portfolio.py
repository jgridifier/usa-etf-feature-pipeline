"""Constrained portfolio optimizer (stub for Milestone 1)."""

from __future__ import annotations

import pandas as pd


def build_portfolio_stub(scores: pd.DataFrame, *_args, **_kwargs) -> pd.DataFrame:
    """Placeholder — growth-mode constrained optimizer in a later milestone."""
    raise NotImplementedError(
        "build-portfolio / constrained optimizer not implemented in Milestone 1; "
        "see config/portfolio_constraints.yaml for planned defaults."
    )
